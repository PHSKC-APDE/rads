% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{bin_age}
\alias{bin_age}
\title{Bin ages into the age categories used for age-standardization}
\usage{
bin_age(age, ref.popname = NULL, cuts = NULL)
}
\arguments{
\item{age}{Numeric vector of indeterminate length. Ages in whole years
(integers), all >= 0. \code{NA} values are allowed and are returned as \code{NA}.}

\item{ref.popname}{Character vector of length 1. The name of a standard
reference population, as returned by \code{\link[=list_ref_pop]{list_ref_pop()}}. Defaults to
\code{"2000 U.S. Std Population (11 age groups)"} (the same default used by
\code{\link[=age_standardize]{age_standardize()}}) when neither \code{ref.popname} nor \code{cuts} is specified.
Cannot be specified together with \code{cuts}.}

\item{cuts}{Numeric vector of indeterminate length (>= 2). The starting age
of each bin, e.g., \code{c(0, 5, 10, 15, ..., 85)}. Must be non-negative whole
numbers with no duplicates. The largest value is treated as the start of
an open-ended top bin (e.g., \code{85} becomes \code{"85+"}). Cannot be specified
together with \code{ref.popname}.}
}
\value{
Character vector, the same length as \code{age}, giving the \code{agecat}
bin to which each element of \code{age} belongs.
}
\description{
Collapses single-year ages into age bins (\code{agecat}), either matching a
standard reference population (see \code{\link[=list_ref_pop]{list_ref_pop()}} / \code{\link[=get_ref_pop]{get_ref_pop()}}) or
a custom set of user-supplied age bins. This is the binning logic used
internally by \code{\link[=age_standardize]{age_standardize()}} when \code{collapse = TRUE}. You can use this
function on its own to align a numerator and a denominator to identical age
bins before calling \code{\link[=adjust_direct]{adjust_direct()}} directly.
}
\details{
At most one of \code{ref.popname} or \code{cuts} may be specified. If neither is
specified, \code{ref.popname} defaults to \code{"2000 U.S. Std Population (11 age groups)"}, matching the default reference population used by \code{\link[=age_standardize]{age_standardize()}}.

When \code{ref.popname} is used, the bins and their \code{agecat} labels exactly
match those returned by \code{get_ref_pop(ref.popname)}, guaranteeing alignment
with the reference population that will be used for age-standardization.

When \code{cuts} is specified, bins are labeled to mimic the styling of RADS's
built-in reference populations, e.g., \code{cuts = c(0, 1, 5, 10)} creates the
bins \code{"0"}, \code{"1-4"}, \code{"5-9"}, and \code{"10+"}.

Ages at or above the largest bin's starting value are placed in that
open-ended top bin. \code{NA} ages, and ages below the smallest bin's starting
value, cannot be assigned to a bin and are returned as \code{NA}, with a warning.
}
\examples{
\donttest{
# Bin ages using the default reference population
bin_age(age = c(0, 3, 10, 45, 90))

# Bin ages using a standard reference population
bin_age(age = c(0, 3, 10, 45, 90),
        ref.popname = "2000 U.S. Std Population (11 age groups)")

# Bin ages using custom cuts
bin_age(age = c(0, 3, 10, 45, 90), cuts = c(0, 1, 5, 10, 20, 40, 65, 85))
}

}
\seealso{
\code{\link[=age_standardize]{age_standardize()}}, which uses \code{bin_age()} internally when
\code{collapse = TRUE}.

\code{\link[=get_ref_pop]{get_ref_pop()}} for the age bins & population counts of a standard
reference population.

\code{\link[=list_ref_pop]{list_ref_pop()}} for a list of valid \code{ref.popname} values for
\code{bin_age()}..
}
