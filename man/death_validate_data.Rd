% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/death_functions.R
\name{death_validate_data}
\alias{death_validate_data}
\title{Validate and prepare death data for use with rads death functions}
\usage{
death_validate_data(
  ph.data = NULL,
  icdcol = "underlying_cod_code",
  check_multicause = FALSE,
  contributing_cols = "record_axis_code",
  verbose = TRUE
)
}
\arguments{
\item{ph.data}{a data.table or data.frame containing line-level death data,
structured with one person per row.

The default is \code{ph.data = NULL}}

\item{icdcol}{a character vector of length one. The name of the column in
\code{ph.data} that contains the underlying cause of death ICD-10 codes.

The default is \code{icdcol = 'underlying_cod_code'}}

\item{check_multicause}{a logical vector of length one. When TRUE, the
function will also validate the contributing cause of death columns required
by \code{\link[=death_multicause_count]{death_multicause_count()}}. These are columns following the naming
pattern \verb{<contributing_cols>_1}, \verb{<contributing_cols>_2}, etc.

The default is \code{check_multicause = FALSE}}

\item{contributing_cols}{a character vector of length one. The stem name of
the contributing cause of death columns in \code{ph.data}. Only used when
\code{check_multicause = TRUE}. The function will look for columns named
\verb{<contributing_cols>_1}, \verb{<contributing_cols>_2}, etc.

The default is \code{contributing_cols = 'record_axis_code'}}

\item{verbose}{a logical vector of length one. When TRUE (default), prints
informational messages about validation results. When FALSE, only shows
warnings and errors.

The default is \code{verbose = TRUE}}
}
\value{
Returns a data.table with the ICD-10 column cleaned via \code{\link[=death_icd10_clean]{death_icd10_clean()}}.
Informative messages, warnings, and errors are printed as appropriate.
}
\description{
Validates that a dataset meets the requirements for use with
\code{\link[=death_113_count]{death_113_count()}}, \code{\link[=death_130_count]{death_130_count()}}, \code{\link[=death_injury_matrix_count]{death_injury_matrix_count()}},
\code{\link[=death_other_count]{death_other_count()}}, and \code{\link[=death_multicause_count]{death_multicause_count()}}. This function
checks for required columns and validates data formats.
}
\details{
This function performs the following validation checks:

\strong{Always checked:}
\itemize{
\item \code{ph.data} is a data.frame or data.table
\item The column specified by \code{icdcol} exists in \code{ph.data}
\item ICD-10 codes in \code{icdcol} are consistent with the expectations of the rads death functions
}

\strong{Checked if present:}
\itemize{
\item \code{chi_geo_kc}: if this column exists in \code{ph.data}, it must contain only
\code{"King County"} or \code{NA}
}

\strong{Checked when \code{check_multicause = TRUE}:}
\itemize{
\item Columns matching \verb{<contributing_cols>_1}, \verb{<contributing_cols>_2}, etc.
must exist in \code{ph.data}
\item A warning is issued if fewer than 20 such columns are found, as typically
20 contributing cause columns are expected
\item ICD-10 codes in `<contributing_cols>_#`` are consistent with the expectations of the rads death functions
}
}
\examples{
# Validate synthetic death data
mydata <- rads.data::synthetic_death
validated_data <- death_validate_data(ph.data = mydata)

# Also validate contributing cause columns for use with death_multicause_count()
validated_data2 <- death_validate_data(ph.data = mydata, check_multicause = TRUE)

}
\seealso{
\itemize{
\item \code{\link[=death_icd10_clean]{death_icd10_clean()}} for ICD-10 code cleaning
\item \code{\link[=death_113_count]{death_113_count()}} for NCHS 113 causes of death counts
\item \code{\link[=death_130_count]{death_130_count()}} for NCHS 130 causes of infant death counts
\item \code{\link[=death_injury_matrix_count]{death_injury_matrix_count()}} for injury matrix counts
\item \code{\link[=death_other_count]{death_other_count()}} for other cause of death counts
\item \code{\link[=death_multicause_count]{death_multicause_count()}} for counts using both underlying and contributing causes
}
}
