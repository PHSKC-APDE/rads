testthat("data_modeler models currently known formats", {

})
testthat("data_modeler handles apostrophie and quote marks", {
  # tet if at least abors and gives notice of why

  # test if gracefully captures and models it


})
